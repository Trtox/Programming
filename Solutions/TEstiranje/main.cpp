#include <iostream>
#include <vector>
using namespace std;

int main()
{
   /* vector<string> k,t;
    string unost,unosk;
    cin>>unost;
    t.push_back(unost);
    cin>>unosk;
    k.push_back(unosk);

    for(auto c : t)
        cout<<c<<endl;
        */
        cout<<25%26;
    return 0;
}
